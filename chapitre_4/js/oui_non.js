/* 
Exercice : jeu du ni oui, ni non
*/
let saisie = prompt("Tu t`appele stephane!");
while ((saisie !=="NON") && (saisie !=="OUI")){
        saisie = prompt("Tu t`appele stephane!")
}
alert("PERDU .......")
